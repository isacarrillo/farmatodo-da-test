resources:
  pipelines:
    farmatodo_de_test_pipeline:
      name: farmatodo_de_test_pipeline
      serverless: true
      channel: "PREVIEW"
      catalog: ${var.catalog}
      schema: ${var.schema}
      root_path: "."
      libraries:
        - glob:
            include: transformations/**
